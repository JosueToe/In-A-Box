import RocketLoader from "./RocketLoader";

export default function RocketLoaderStoryboard() {
  return (
    <div className="bg-gradient-to-b from-blue-900 via-indigo-900 to-purple-900 min-h-screen flex items-center justify-center">
      <RocketLoader />
    </div>
  );
}
